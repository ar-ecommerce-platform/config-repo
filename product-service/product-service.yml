---
# Served by config-server (Spring Cloud Config, native backend). Each service also
# ships a self-contained application.yml, so this file is supplementary.
spring:
  application:
    name: product-service
server:
  port: 8083
demo:
  config-source: config-server
